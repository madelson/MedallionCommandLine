* smart printer (can we get console width?)
* commands have 
	* options
		* name, alias
		* isflag
		* default
		* 
	* arguments
		* optional?
		* type
		* validator
	* subcommands
		* these are full commands!


* help option
* styles (e. g. -- /Q, ..., nuget style (single dash))
* --t=xxx, other styles for option args from docopt
* defaults
* Path validation (Path.GetFullPath)
* Consider rename of Option/Arg to CommandOption, CommandArg to avoid public pollution
* Consider no mixing of positional and options