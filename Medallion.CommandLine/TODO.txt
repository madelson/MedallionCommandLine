* smart printer (can we get console width?)
* commands have 
	* options
		* name, alias
		* isflag
		* default
		* 
	* arguments
		* optional?
		* type
		* validator
	* subcommands
		* these are full commands!


* help option
* styles (e. g. -- /Q, ...)
* defaults